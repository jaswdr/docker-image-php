sudo: required

language: php

services:
  - docker

script:
  - docker build -t jaschweder/php .

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push jaschweder/ubuntu-nginx-php;
    fi
