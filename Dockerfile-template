FROM {IMAGE_FROM}

MAINTAINER Jonathan A. Schweder "jonathanschweder@gmail.com"

RUN composer global require edgedesign/phpqa --update-no-dev

ENTRYPOINT ["phpqa"]

CMD ["--analyzedDirs", "./", "--buildDir", "./build", "--ignoredDirs", "bootstrap,config,database,public,resources,routes,storage,tests,vendor", "--verbose", "--report"]
